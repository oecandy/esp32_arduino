void pin_setup(){
  pinMode(RELAY_PUMP,OUTPUT);
  pinMode(RELAY_FAN,OUTPUT);
  pinMode(RELAY_SWING,OUTPUT);
  pinMode(RELAY_LIFTING,OUTPUT);
  pinMode(RELAY_EXTRA1,OUTPUT);
  pinMode(RELAY_EXTRA2,OUTPUT);
}
